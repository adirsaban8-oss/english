{
  "name": "linking-words-game",
  "version": "1.0.0",
  "description": "English linking words game for matriculation exam",
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
